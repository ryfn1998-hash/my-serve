name: Auto Start Falix Server

on:
  repository_dispatch:
    types: [start-command]

jobs:
  start_server:
    runs-on: ubuntu-latest
    steps:
      - name: Send Start Signal to Falix
        run: |
          curl -X POST "https://client.falixnodes.net/api/client/servers/你的伺服器ID/power" \
          -H "Authorization: Bearer ${{ secrets.FALIX_TOKEN }}" \
          -H "Content-Type: application/json" \
          -H "Accept: application/json" \
          -d '{"signal": "start"}'
